# @package _global_
# 2026-04-29: small-batch convergence test. Identical to
# training_repa_l4_128_per_residue_bs80.yaml except batch_size=24 (via dataset
# pdb_lmdb_128_bs24) and a fresh run_name. Tests whether bs24 reaches better
# loss faster than bs80 at matched wall time (12h).
defaults:
  - /training_ca
  - _self_

run_name_: proteina_60m_repa_l4_128_per_residue_bs24

dataset: pdb_lmdb_128_bs24
dataset_config_subdir: pdb

compile: true

log:
  wandb_project: proteina-repa
  checkpoint_every_n_steps: 100000
  last_ckpt_every_n_steps: 3500

ema:
  validate_original_weights: True

repa:
  encoder:
    type: gearnet
    gearnet_ckpt_path: ${oc.env:DATA_PATH}/metric_factory/model_weights/gearnet_ca.pth
  layers: [4]
  lambda_repa: 0.5
  combination_mode: additive
  similarity_type: cosine
  averaging: per_residue
  projector_hidden_dim: 512
  projector_num_layers: 3  # reference REPA depth (divergence from 256/512 configs which use 2)

eval_callback:
  enabled: false
  compute_every_n_steps: 50000
  num_samples: 30
  length: 100
  compute_gearnet: true
  compute_designability: false
  compute_diversity: true
  gearnet_ckpt_path: ${oc.env:DATA_PATH}/metric_factory/model_weights/gearnet_ca.pth
