# @package _global_
defaults:
  - inference_fid_60m_repa
  - _self_

run_name_: eval_60m_repa_lite

# Lite eval: coarser length grid, fewer samples (~300 PDBs, ~35 min vs 12h)
step_len: 40
nsamples_per_len: 25
