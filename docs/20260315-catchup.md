What do I have so far?

60MM model, 12 layers, unconditional generation
1. generating 100 residue proteins -- layer 0 is where the bias content appears used most energetic relative to the content. Does that mean anything?
Does that just mean the content matrix has low norm because this is where it is initialised? -> no this holds across time
2. bias matters: contact prediction using attn(B) >> attn(C). But is this even too much of a difference? 0.25 vs 0.05
3. Looking at layer 0, local index-based sequence separation bias logit dominance >> content

==

Framing for the work in intro/motivation
'Where' and 'when' does geometric bias shape the flow-matching denoising trajectory?
- where = model depth
- when = model time (on an aside let's be consistent about using early/late for time and shallow/deep for model depth, maybe we should say in the report that we will be using this shorthand)
Is this different for unconditional vs conditional generation?
In this report we discover
- unconditional: despite descriptive analyses perhaps suggesting otherwise, the model appears to retain no memory o

Lenses to study: Across space (layer depth) and time (generation time):
1. Descriptive: How much softmax-relevant content do B and C have?
2. Causal: What happens when we remove B?
3. Functional: Do these components produce structurally useful attention? (Following Rao et al)
4. Representation: How do the model representions map to realistic 3d structures?
-> we use these metrics to understand what's happening in the model
1. R vs Rc <- descriptive lens
2. Precision@L/5 <- functional lens, self referential in the unconditional case
3. Contact similarity, Kabsch RMSD <- structural lens, self referential in the unconditional case

Evaluation
Metrics to measure quality of unconditional output
1. structural quality: rg, steric 
2. protein-like nature: (GearNet classification confidence)
- These metrics are used in the proteina paper (maybe address that the other metrics are probability distribution based. Open q: Shuold we do this as well? We can generate a lot more than n=3 or 5 since the compute is not very expensive?)

Results presentation
- I want the tables and plots to sort of speak for themselves as much as possible, ideally you dont even need to read the text to udnersstand what is happening
1. First show descriptive lens
- peak layer phenomenon suggesting that there 'where' of B works differently in different models -- we should show what is currently figure 4 here, followed by what is currently figure 3 to illustrate the point about different geometric interpreter layers Rc in diff models and how they work 
- I dont think the R vs Rc discussion is even necessary here -- or include in appendix
- Does this section say much about the 'when' of B's utility? I dont think so. We should make a point here one way or the other

2. However ablation reveals the dominant that holds true across models: temporal asymmetry independent of spatial pattern
- we should wrap the content in 'Sharp temporal asymmetry' into 'The model has no memory of early B.' as is the headline takeaway. figure 1 should show up first and should contain the data across all model types! Please re run if necessary to get this data. This feels the 'money plot' of our report to me.
- I think what is currently Table 4 needs to be the only table for this section and everything can be wrapped into it really. Maybe we can separate out sections. eg what is currently table 3 xan have a haeder saying 'temporal ablation', perhaps along with the early/late ablations. While what is currently table 5 can have a header saying 'geometric content ablation'. While the spatial ablations can be called as such as well.
- Table 4 early and late should say the t value threshold
- Why is there no shallow ablation? And why dont we include the peak Rc layer abloation result here? Just wrap what is currently table 6 into what is currently table 4
- I think there's a point to be made here about 'it does not matter where you ablate, all that matters is when'. Is that correct? What is the smoking gun evidence that shows this if true?
- I dont understand the point of the bolded figures in table 4 rn
- why doesnt tabel 3 have figures for all models? We should run _every_ experiment across all 4 models!!
- please make sure the table and figure descriptions/captions also highlight what the takeaway is
- 'Ablated structures are either valid folds or collapsed — no intermediate regime.' - where are the results? presumably if we report the CATH prediction score along with Rg and clash everywhere then we can point to it. I'm not yet convinced this section adds anything yet, but let's give it a shot and edit later if need be.
- Can we wrap the 'Geometric information not magnitude.' data from table 5 into table 4? I think we only need to have one table in this section really
- 'Scale and length invariance.' seems a good validation point. Maybe we can phrase it as such. This can be a separate table. It doesnt look like we ran the gap ablation 

Overall I think:
1. run the experiemts for all models
2. move everything into one table as much as possible
2. Add the third eval metric in the prediction confidence to the table
If we need to re run experiemnts for this, let's do that

3. The functional and representational pieces can get a quick plug. Given the tautological nature of the claim there's not too much interesting here in my opinion, beyond the finding that the intermediate representations are less structually relevant than the beginning and end. Perhaps what we really need to do is show these results in the context of ablations ie what happens to these when we remove/modify B at each step. But I think that will need a whole different set of experimetns. Let's organise what we have right now into a more cohesive narrative and then work on this after.

Let's remember we are telling a data driven story. This is an investigation and should read liek one with an interesting takeaway if possibke. But we should make up absolutely bothung. Always keep the bar of smoking gun evidence for every claim we make and make sure you verify that we are sticking true to that.