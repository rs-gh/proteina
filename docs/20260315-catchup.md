What do I have so far?

60MM model, 12 layers, unconditional generation
1. generating 100 residue proteins -- layer 0 is where the bias content appears used most energetic relative to the content. Does that mean anything?
Does that just mean the content matrix has low norm because this is where it is initialised? -> no this holds across time
2. bias matters: contact prediction using attn(B) >> attn(C). But is this even too much of a difference? 0.25 vs 0.05
3. Looking at layer 0, local index-based sequence separation bias logit dominance >> content