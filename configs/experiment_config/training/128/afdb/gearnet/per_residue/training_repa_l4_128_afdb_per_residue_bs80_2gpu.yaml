# @package _global_
# AFDB n=128 REPA aligned to CA-GearNet L4, effective bs=80 via 2-GPU DDP.
# Topology: per_gpu_bs=40 * 2 GPUs * accumulate_grad_batches=1 = 80.
# Mirrors training_repa_l4_128_afdb_per_residue.yaml but with 2-GPU bs=80.
defaults:
  - /training_ca
  - _self_

run_name_: proteina_60m_repa_l4_128_afdb_per_residue_bs80_2gpu

dataset: afdb_swissprot_lmdb_128_bs40
dataset_config_subdir: afdb

compile: true

hardware:
  ngpus_per_node_: 2

opt:
  val_check_interval: 10000000
  accumulate_grad_batches: 1

log:
  wandb_project: proteina-repa
  checkpoint_every_n_steps: 100000
  last_ckpt_every_n_steps: 3500

ema:
  validate_original_weights: True

repa:
  encoder:
    type: gearnet
    gearnet_ckpt_path: ${oc.env:DATA_PATH}/metric_factory/model_weights/gearnet_ca.pth
  layers: [4]
  lambda_repa: 0.5
  combination_mode: additive
  similarity_type: cosine
  averaging: per_residue
  projector_hidden_dim: 512
  projector_num_layers: 3

eval_callback:
  enabled: false
  compute_every_n_steps: 50000
  num_samples: 30
  length: 100
  compute_gearnet: true
  compute_designability: false
  compute_diversity: true
  gearnet_ckpt_path: ${oc.env:DATA_PATH}/metric_factory/model_weights/gearnet_ca.pth
